# 文件路径：.env.example
# -*- coding: utf-8 -*-
# 项目环境变量示例，复制此文件为 .env 并填写实际值

##########################################
# 一、Redis 相关配置
##########################################
# Redis 服务地址（若本机部署则为 127.0.0.1）
REDIS_HOST=127.0.0.1
# Redis 服务端口号，默认为 6379
REDIS_PORT=6379
# Redis 数据库索引，默认为 0
REDIS_DB=0

##########################################
# 二、API 鉴权配置
##########################################
# 项目 API Key 列表（逗号分隔），必须设置且至少一个
API_KEYS=your_api_key_here

##########################################
# 三、告警阈值配置
##########################################
# Redis 队列长度告警阈值（列表长度 ≥ 阈值时触发告警）
QUEUE_ALERT_THRESHOLD=1000
# 节点故障率告警阈值（失败率 ≥ 阈值时触发告警，0.1 = 10%）
FAILURE_RATE_THRESHOLD=0.1

##########################################
# 四、告警渠道选择
##########################################
# local: 本地邮件 + 钉钉；cloud: 云端告警
ALERT_PROVIDER=local

##########################################
# 五、SMTP 邮件告警配置
##########################################
# SMTP 服务地址（如 QQ 邮箱：smtp.qq.com）
SMTP_SERVER=
# SMTP 端口号（一般为 587 或 465）
SMTP_PORT=587
# 发件邮箱账号（如 QQ 邮箱地址）
SMTP_USER=
# 发件邮箱授权码（非登录密码）
SMTP_PASSWORD=
# 收件人列表，使用逗号分隔多个邮箱
ALERT_EMAIL_LIST=admin@example.com

##########################################
# 六、钉钉机器人告警配置（可选）
##########################################
# 钉钉群自定义机器人 Webhook 地址
DINGTALK_WEBHOOK=
# 钉钉机器人加签密钥（若启用加签，则必填）
DINGTALK_SECRET=

##########################################
# 七、FastAPI 服务相关配置
##########################################
# FastAPI 服务监听主机，默认为 0.0.0.0 可对外暴露
API_HOST=0.0.0.0
# FastAPI 服务监听端口，默认为 8000
API_PORT=8000

##########################################
# 八、定时任务配置
##########################################
# 后台监控任务循环间隔（秒）
JOB_INTERVAL_SECONDS=60

##########################################
# 九、Prometheus 指标埋点配置（可选）
##########################################
# true: 启用 Prometheus 指标并暴露 /metrics；false: 不启用
PROMETHEUS_METRICS_ENABLED=false

##########################################
# 十、云端告警 Webhook（当 ALERT_PROVIDER=cloud 时使用）
##########################################
# CLOUD_ALERT_WEBHOOK=